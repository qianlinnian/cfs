#include<iostream>
#include<string>
#include<algorithm>
using namespace std;

int main()
{
    int t;
    while(t--)
    {
        int num;
        cin>>num;
        string nums;
        nums=to_string(num);
        int len=nums.length();
        int sum=0;
    }
    return 0;
}